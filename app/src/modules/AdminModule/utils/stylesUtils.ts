import { TextStyle, ViewStyle } from "react-native"

export const iconStyle: TextStyle = {
    fontSize: 50
}

export const fabBtnStyles: ViewStyle = {
    position: 'absolute',
    right: 10,
    bottom: 55,
}